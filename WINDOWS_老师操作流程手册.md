# 作文批改台 Windows 老师操作流程手册

适用场景：老师在 Windows 电脑本地运行作文批改台，把学生作文全文复制粘贴到工作台进行批改。

## 一、首次准备

### 1. 解压程序包

请先把压缩包完整解压，例如放到：

```text
桌面\作文批改台-本地版
```

不要直接在压缩包内运行文件。

### 2. 安装 Node.js LTS

1. 打开 <https://nodejs.org/zh-cn/download>。
2. 下载 Windows 的 LTS 安装包（`.msi`）。
3. 双击安装，保持默认选项，并保留加入 PATH 的选项。
4. 安装完成后重新打开作文批改台文件夹。

可按 `Win + R`，输入 `cmd`，再运行：

```bat
node -v
```

出现版本号即表示安装成功。

## 二、启动与停止

双击：

```text
start-local-windows.bat
```

浏览器通常会自动打开：

```text
http://127.0.0.1:8787/index.html
```

启动后出现的黑色命令窗口要保持打开。关闭该窗口或双击 `stop-local-windows.bat`，本地服务就会停止。

注意：Windows 不要打开 `.command` 文件，它是 macOS 使用的启动文件。

## 三、配置批改模型

1. 点击“模型配置”。
2. 选择服务商与文本模型。
3. 填写 API Key 和 Base URL。
4. 点击“测试连接”。
5. 测试成功后点击“保存模型配置”。

当前流程不使用 OCR 或视觉模型。API Key 只用于作文批改与全文润色。

## 四、批改一篇作文

1. 点击左侧“+ 新建”。
2. 在顶部填写学生姓名。
3. 选择教材题目，或点击“自定义题目”填写题目和要求。
4. 按需修改“批改前提示词”。
5. 将学生作文全文粘贴到“作文正文”。
6. 如需在 PDF 中附上原稿，可点击“添加报告图片（可选）”；图片不参与识别、定位或批改。
7. 点击“批改”。
8. 查看首页、点评、升格和润色内容。
9. 审核完成后点击打印按钮，导出 PDF。

正文、姓名、题目、提示词和报告都保存在当前任务中。刷新页面或从其他页面返回，不会清除当前工作。

## 五、同时处理多篇作文

1. 提交第一篇作文批改后，直接点击“+ 新建”。
2. 粘贴第二篇作文并再次点击“批改”。
3. 可继续建立第三篇、第四篇任务。
4. 点击左侧任务卡可随时切换，查看每篇作文的正文、状态和报告。

每个任务独立保存，互不覆盖。任务状态包括“编辑中”“待批改”“批改中”“已完成”和“批改失败”。

如果模型账号只允许一个并发请求，多篇作文仍可依次提交，后台会排队调用模型；已经提交的任务不会阻碍老师继续录入下一篇。

## 六、数据保存规则

- 刷新网页：数据保留。
- 从其他页面返回工作台：数据保留。
- 切换任务：各任务内容独立保留。
- 重新双击启动器开始一次新的工作：清空上次工作区，从空白任务开始。

本地数据位于：

```text
backend\data\app-data.json
```

不要在批改过程中删除 `backend\data` 文件夹。

## 七、常见问题

### 无法打开网页

确认黑色命令窗口仍在运行，然后手动访问：

```text
http://127.0.0.1:8787/index.html
```

### 提示没有 Node.js

重新安装 Node.js LTS，安装后关闭旧命令窗口，再重新双击启动文件。

### 提示端口被占用

先双击 `stop-local-windows.bat`，再重新启动。

### 批改失败

依次检查 API Key、模型名称、Base URL、网络连接和模型账户余额。若提示输出长度不足，可重新提交，或把过长的个性化提示词适当精简。

### 电脑休眠后还能批改吗

不能。电脑休眠时本地服务和正在执行的请求会暂停。批改期间建议接通电源并暂时关闭自动休眠。

## 八、一句话操作

安装 Node.js LTS，完整解压程序包，双击 `start-local-windows.bat`，在网页里新建任务、填写姓名和题目、粘贴作文全文，然后点击“批改”；第一篇运行时可以继续新建并提交下一篇。
