# 作文批改台 Windows 老师操作流程手册

适用场景：老师电脑是 Windows，本地运行作文批改台；手机和电脑在同一个局域网内时，可以用手机拍照上传到电脑端。

## 一、准备工作

### 1. 准备安装包

拿到安装包后，建议先把压缩包完整解压，不要直接在压缩包里双击运行。

推荐放置位置：

```text
桌面\作文批改台-本地版
```

文件夹里应能看到这些文件：

```text
start-local-windows.bat
stop-local-windows.bat
index.html
capture.html
backend
app.js
styles.css
package.json
```

### 2. 安装 Node.js LTS

1. 打开 Node.js 官方下载页：https://nodejs.org/zh-cn/download
2. 选择 Windows 版本的 LTS 安装包。
3. 下载 `.msi` 安装文件后双击安装。
4. 安装过程中保持默认选项即可，尤其要保留“Add to PATH”相关选项。
5. 安装完成后，重新打开作文批改台文件夹。

如需确认是否安装成功：

1. 按 `Win + R`。
2. 输入 `cmd`，回车。
3. 输入：

```bat
node -v
```

如果能看到类似 `v24.x.x` 或 `v22.x.x` 的版本号，就说明安装成功。

## 二、启动电脑端批改台

1. 打开作文批改台文件夹。
2. 双击：

```text
start-local-windows.bat
```

不要双击 `.command` 文件。`.command` 是 macOS 使用的启动文件，Windows 打开它时会提示“选择打开的软件”，这是正常现象，不代表程序坏了。

3. 如果 Windows 防火墙弹窗询问是否允许访问，请选择允许，至少勾选“专用网络”。
4. 正常情况下浏览器会自动打开：

```text
http://127.0.0.1:8787/index.html
```

5. 启动后会出现一个黑色命令窗口。这个窗口要保持打开，关闭窗口后本地服务会停止。

如果浏览器没有自动打开，可以手动复制下面地址到浏览器：

```text
http://127.0.0.1:8787/index.html
```

## 三、首次配置模型 API

1. 在网页顶部点击“模型配置”。
2. 选择服务商，例如 DeepSeek、Kimi 等。
3. 填写对应的 API Key。
4. 点击“测试连接”。
5. 测试通过后，点击“保存模型配置”。

注意：

- API Key 保存在老师电脑本地。
- 如果没有配置 API Key，批改、图片文字读取等大模型功能不能正常使用。
- 如果模型连接失败，优先检查 API Key、网络、代理/VPN、防火墙。

## 四、电脑端直接上传作文图片

适合老师已经把照片传到电脑上的情况。

1. 打开“批改工作台”。
2. 在“姓名”处填写学生姓名。
3. 选择年级、册次、单元和作文题目；也可以使用“自定义题目”。
4. 点击“上传原文图片”。
5. 选择一张或多张作文照片。
6. 点击“识别图片”，生成识别文本。
7. 在识别文本区检查并微调文字。
8. 点击“批改”。
9. 查看“点评”“升格”“润色”等内容。
10. 老师可以删除不需要的点评、拖动批注点、修改润色文本。
11. 点击打印按钮导出或打印 PDF 报告。

## 五、手机拍照上传到电脑端

适合老师直接用手机拍学生作文。

### 1. 连接同一个网络

手机和 Windows 电脑必须连接同一个 Wi-Fi。

如果学校 Wi-Fi 有设备隔离，手机可能无法访问电脑。这种情况下建议：

- 让电脑和手机连接同一个普通路由器；
- 或者电脑开热点，手机连接电脑热点；
- 或者手机开热点，电脑连接手机热点。

### 2. 打开手机拍照页

电脑端页面会显示手机拍照地址，通常类似：

```text
http://192.168.x.x:8787/capture.html
```

请在手机浏览器里打开这个地址。

注意：

- 手机上不要输入 `127.0.0.1:8787`，这个地址只代表手机自己。
- 手机必须使用电脑页面显示的局域网地址，例如 `192.168.2.106:8787/capture.html`。

### 3. 手机端提交

1. 手机打开拍照页。
2. 填写学生姓名。
3. 选择作文任务。
4. 点击拍照区域。
5. 可以连续拍多张作文照片。
6. 点击“提交到 Web”。
7. 回到电脑端，待处理任务会自动出现。
8. 点击对应任务后，图片会进入电脑端工作台。

## 六、正常批改流程

推荐顺序：

```text
填写姓名
选择或自定义作文题目
上传/接收作文图片
识别图片
校对识别文本
点击批改
老师审核点评、升格、润色
导出 PDF
```

重点提醒：

- 识别文本区是最终送给批改模型的正文，老师可以在这里修正识别错误。
- 原始图片不要删除，报告和批注定位会用到原图。
- 如果学生字迹很潦草，可以先用其他大模型网页识别出文字，再把文字复制到识别文本区，但仍建议保留原始图片。

## 七、停止服务

有两种方式：

方式一：关闭启动时打开的黑色命令窗口。

方式二：双击：

```text
stop-local-windows.bat
```

## 八、数据保存和备份

老师的数据保存在本地文件：

```text
backend\data\app-data.json
```

它可能包含：

- 模型配置；
- API Key；
- 待处理任务；
- 已上传图片；
- 批改结果；
- 老师修改后的内容。

不要随便删除 `backend\data` 文件夹。

如果要备份，最简单的方式是把整个“作文批改台-本地版”文件夹复制一份。

如果要换新版本，可以把旧版本里的：

```text
backend\data
```

复制到新版本同样位置。

## 九、常见问题

### 1. 双击启动后提示没有 Node.js

说明电脑没有安装 Node.js，或者安装后没有加入 PATH。

处理方式：

1. 重新安装 Node.js LTS。
2. 安装完成后关闭所有命令窗口。
3. 重新双击 `start-local-windows.bat`。

### 2. 手机打不开拍照页

优先检查：

- 手机和电脑是否在同一个 Wi-Fi；
- 手机打开的是不是 `192.168.x.x:8787/capture.html`；
- 电脑是否休眠；
- 黑色启动窗口是否还开着；
- Windows 防火墙是否允许 Node.js 访问网络；
- 学校网络是否开启了设备隔离。

### 3. 电脑端打不开网页

尝试手动打开：

```text
http://127.0.0.1:8787/index.html
```

如果仍打不开：

1. 双击 `stop-local-windows.bat`。
2. 再双击 `start-local-windows.bat`。
3. 确认黑色窗口里没有明显报错。

### 4. 提示端口被占用

先双击：

```text
stop-local-windows.bat
```

然后重新双击：

```text
start-local-windows.bat
```

### 5. 批改失败或模型报错

依次检查：

- API Key 是否填写正确；
- 模型服务商是否选对；
- Base URL 是否正确；
- 电脑是否能正常访问网络；
- 账户余额是否充足；
- 是否使用了公司或学校代理/VPN。

### 6. 手机提交后电脑端没有出现任务

处理方式：

1. 确认手机提交时显示成功。
2. 回到电脑端批改工作台。
3. 等待几秒或刷新电脑端页面。
4. 确认电脑启动窗口没有关闭。
5. 如果仍不出现，先用电脑端“上传原文图片”临时处理。

### 7. 电脑休眠后还能用吗

不能。电脑休眠后，本地服务会暂停，手机无法上传，电脑网页也可能断开。

建议批改期间：

- 保持电脑亮屏或不休眠；
- 关闭合盖休眠；
- 接通电源。

## 十、给老师的一句话版本

先安装 Node.js LTS，解压作文批改台文件夹，双击 `start-local-windows.bat`，保持黑色窗口打开；电脑端用 `127.0.0.1:8787`，手机端用网页显示的 `192.168.x.x:8787/capture.html`，手机和电脑必须在同一个 Wi-Fi。
